# Assignment 2 Codebase

Place all PyTorch code for Assignment 2 specialized track here:
- `dataset.py`: Large-scale dataset loader with leakage prevention splits
- `models/`: Simple baseline & pretrained backbone implementations
- `train.py`: Fine-tuning pipeline & controlled factor experiments
- `evaluate.py`: Task-specific evaluation (mAP, mIoU, Dice, Rank-1/5, Abs Rel, RMSE)
